apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team-foresters
  namespace: argocd
  labels:
    kcdporto: teams
    team: foresters
    cost-center: ll-405
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:infbase/kcdporto2025
    targetRevision: main
    path: applications/teams/foresters
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: foresters-team
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

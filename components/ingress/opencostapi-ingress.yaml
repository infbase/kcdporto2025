apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencostapi
  namespace: opencost
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.opencost.kcd.eso.pt
      secretName: opencostapi-tls
  rules:
    - host: api.opencost.kcd.eso.pt
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: opencost
                port:
                  number: 9003
